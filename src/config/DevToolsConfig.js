if (__DEV__) {
  require('react-devtools');
}

/**
 * >> adicionar ao app.js
 * import './config/DevToolsConfig' //verificar caminho
 * >> digitar
 * $ yarn add react-devtools --dev
 * 
 * por fim no package.json adicionar...
 * "scripts": {
 *   ...,
 *   "react-devtool": "react-devtools",
 * }
 * para executar no terminal digitar
 * $ yarn run react-devtool
 */
